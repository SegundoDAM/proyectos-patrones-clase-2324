package kata;

import java.util.Queue;

public class KataStream {
	/**
	 * counts the number of 
	 * values ​​greater than X that appear in the first
	 *  Z values ​​of the queue
	 * @param numbers the collection
	 * @param initialNumber minimun to find
	 * @param elements the last element to search to
	 * @return How many elements meet the condition
	 */
	public long howManyNumbersBiggerThanXbeforeZ(Queue<Integer> numbers, int initialNumber, int elements) {
		return -1;
	}
}
